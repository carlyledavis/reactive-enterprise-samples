package messaging;

import models.EmailAddress;
import models.EmailConfirmation;
import models.Reservation;

public class MessagingProvider {
    public EmailConfirmation sendEmail(Reservation reservation, EmailAddress emailAddress) {
        return null;
    }
}
